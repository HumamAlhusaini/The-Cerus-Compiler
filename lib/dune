

(menhir
  (modules parser/tokens parser/expr parser/items parser/program parser/stmt parser/types)
  (merge_into program)
)

(library
 (name Lib)
 (modules program lexer pprinter ast)
 )

(ocamllex lexer)

